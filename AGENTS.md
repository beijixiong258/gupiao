# 项目级开发准则

本文件是后续会话和其他智能体开发本项目时必须优先遵守的长期约束。

## 智能体优先，而非固定工作流

- 本项目应保持“智能体负责理解、审查与决策，量化程序负责可复现计算”的边界。不要把用户可能说出的行业、板块、概念或同义词写成静态业务枚举，也不要用不断增长的 `if/else` 模拟自然语言理解。
- 当用户用日常语言描述选股范围时，先动态访问当前数据源的行业和概念目录，再把真实候选、来源、取得时间和核验信息交给智能体结合上下文判断。唯一且经过核验的精确候选可以合理采用；多个真实候选仍无法根据上下文判断时，只向用户澄清一次。
- 智能体不能盲信单次 API 响应。对会改变候选池的关键事实，应使用同一权威来源的详情接口或另一可靠来源进行二次核验；无法核验、来源降级或候选冲突必须明确进入结构化结果，由智能体据此决定继续、澄清或拒绝给出当前结论。
- 用户不需要理解 `industry`、`board`、数据源代码、工具参数等专业实现词。智能体负责把这些内部事实翻译成普通中文。
- AI 的判断空间用于：理解用户真实意图、评估候选歧义、检查数据来源和降级是否足以支撑结论、解释量化证据及风险。可复现的行情处理、硬过滤、因子、形态、评分和门槛必须由程序计算；AI 不得修改数值、编造概率或绕过风险规则。
- 用户点名单股时，程序必须建立有界比较池并复用统一选股的八组因子、基本面、形态、尾盘、风险扣分、综合评分和推荐门槛，最终固定输出“建议买入”或“暂不建议买入”；单股适配层和 AI 都不得另造或修改分数。结尾还必须基于已计算证据给出一段普通人能直接理解的收束总结：承认真实亮点、解释当前为何买或不买，并列出重新评估条件。单股买入结论不产生预测资格。

## 数据与预测边界

- 市场数据现取现用，只允许一次请求内的内存复用；不得把行情、板块目录、成分、因子或预测输入持久化为本地市场数据缓存。
- 量化分析是默认主流程。只有分析完成并出现合格候选后，智能体才主动说明预测成本并再次询问；用户在后续回合明确确认后才能进入预测。
- 当前分析改造不得改变预测模型算法、训练口径和 T+1/T+2/T+3 语义。跨进程状态失效时，应重新获取远端数据并重新分析，不得读取旧市场缓存伪装恢复。

## 工程质量

- 依赖方向保持清晰：数据源适配与重试、领域值对象、候选池策略、量化编排、对话展示分层；优先复用、小型数据类和组合，避免平台化过度设计。
- 结构化区分：分析成功、没有合格候选、需要澄清、远端数据不可用、程序错误。任何 `unavailable` 都不得被外层标记为业务成功。
- 新增关键来源或规则时同步更新长期文档。开发产生的一次性脚本、下载目录、素材和产物在交付前清理。
