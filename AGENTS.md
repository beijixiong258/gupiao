# 项目级开发准则

本文件记录后续会话和其他智能体开发本项目时应遵守的长期约束。当前业务为单股完整证据分析与范围规则选股；用户最新明确要求优先于历史设计。

## 应用场景与最新产品目标

- 选股的明确目标是选出截至提问时，在用户给定范围和要求中最优秀的个股，并用真实量化证据说明优选理由。比较与复核应服务于完成优选；实际覆盖不足或证据不足须如实说明，不能把未完成的比较包装成已找到范围内最优。
- 用户没有提出风险要求时，不主动询问风险偏好，不默认以低风险、低波动为选股目标，也不替用户追加风险过滤条件。用户明确提出后再纳入相应限制。客观指标和数据有效性核验继续保留；指标存在不意味着自动采用某种风险偏好。
- 单股分析负责检验用户的猜想，尽量使用严谨、可复现、可解释的量化证据，给出支持、反证、缺口与明确的综合判断，说明哪些变化会强化或推翻判断。没有明确猜想时，完整评价本股已有表现。
- 两项功能均依据截至提问时已取得并核验的表现与资料，不重新引入模型训练或未经验证的上涨概率。
- 当前实现按20日、5日已实现表现作非支配分层，同层明确优先20日表现、再5日表现；完全相同才按代码稳定显示并标明并列。范围分批全量请求，技术复核完成后才裁剪展示。最优必须说明这一技术表现口径与实际覆盖，不能偷换为已知未来收益最优或内在价值最优。

## 智能体与计算程序的边界

- 智能体负责理解用户意图、评估真实候选歧义、审查来源与降级、解释证据和冲突。量化程序负责行情处理、原始指标、形态、可交易性、显式条件和非支配分层；智能体不得改写数值、编造概率或绕过规则。
- 不把行业、板块、概念和同义词写成静态业务枚举，也不以不断增长的 `if/else` 模拟自然语言理解。
- 用户描述范围时，动态访问当前目录，提供真实候选、来源、取得时间和核验信息。唯一且经过核验的精确候选可以采用；多个真实候选仍无法从上下文判断时，只澄清一次。
- 会改变候选池的关键事实必须用权威详情接口或另一可靠来源核验。未核验、来源降级和冲突保留在结构化结果中，不能把失败当成排除候选的证据。
- 用户不需要理解数据源分类、工具参数或分析编号。智能体应把内部事实译成普通中文。

## 技术分析主导与解释边界

- 以量化和技术证据为诊断主轴，结合价格结构与位置、趋势与动能、量价与流动性、指数和同行背景、波动与下行风险作综合解释。公告、财报和资讯作为有来源、有时点的补充核验，不把主流程变成新闻摘要；未取得的资讯不得假装查过，事件发生与股价变化的因果解释必须区分。
- 程序负责计算和信号适用、确认、过期、失效等可复现状态，智能体在这些约束内解释。过期、失效、不适用的历史观察可保留于完整报告，不得作为当前信号；待确认不能当作已成立，缺失不能补猜，真实反证不能为迎合结论而忽略。
- 同一观测的别名、排名、标准化和相关变换不算独立确认，不按指标数量投票。不同日线窗口不冒充已核验的周线或分钟结构；指数与比较池方向冲突不直接等于个股横盘；超买、超卖或单次交叉不单独决定买卖结论。
- “看准”落实为数据、口径、时点、信号状态和解释有依据。历史完整只说明计算基础较充分，不代表未来收益有保证，也不能代替实际历史验证得出命中率。
- 内部设问、推演、待验证假设和工具计划不对用户逐项展示。公开回答给出结论、关键事实、实质反证、证据缺口和有依据的复评条件；用户追问原因时提供可核验的依据摘要与计算说明，不输出内部思维链。
- 全量提取与重点解释并存：完整指标、数值和缺失原因仍可访问，正文按当前问题组织有效证据，不逐个指标机械解说，也不以精简为由隐藏重要风险。该解释层不得改写程序数值、选股条件或候选次序。
- 形态期限按程序返回的规则口径解释：基准涨停日为 D0，缩量后突破仍受从同一 D0 起算的截止日约束，缩量不能重置期限。只有原始 PE/PB、缺少有效可比样本时，不断言估值高低；动态、滚动和其他口径明确区分。

## 两项功能

- 点名单股时，尽量提取这只股票全部可分析证据，不套用范围选股过滤器把目标先排除。风险警示、上市时长、成交限制等作为报告证据保留，不强制输出买入或不买入。
- 单股共享八组原始指标、完整技术、基本面与估值、形态、尾盘、七类补充诊断和来源。每项已有数值、单位、中文含义和缺失都可访问，不固定只展示前几项。摘要只解释已算事实，说明支持证据、反证、限制和复评条件。
- 同行、指数、财务、实时或分钟证据最终仍不可用时，继续展示本股已有内容并标为 `partial / information_partial`。指数备用源已通过同样质量核验时，保留主源失败及降级记录，不将已恢复的证据继续列作缺失。短历史按各指标窗口说明不足，不设置额外单股最低历史行数或最低同行数来阻断全部报告。必要身份、交易日期或目标日线不可用，以及程序错误，分别保留真实状态。
- 范围默认只要求有效身份、完整分析日、有效价量、20日表现所需连续窗口（21条价格）、技术原值一致性及适用的成交核验。ST、上市时长、最低成交额大小、波动上限、全部上涨或放量等限制须由用户明确提出，经 `tiaojian` 记录并逐项核验。指标缺失不是通过；低成交额观察基准只供解释，不是默认硬过滤。
- 市值限制须由智能体显式传入总市值或流通市值口径、绝对上限及严格/含上限边界；“小盘股”口径或上限不明先返回澄清，本轮停止取数与候选报告补全。市值条件在分批历史取数前执行，当前仅使用同日已核验的日终估值，未核验者单列为证据不足，不计作超标；不自动发明默认阈值，不把百分位请求改成样本百分位。澄清结果不能附加此前候选。
- 20日和5日窗口相关，不作独立投票；同日同基准的20日超额不重复参与排序。均线、MACD、量价与形态解释上下文和反证，不默认把数值越大视为越好。保留与后一候选的原值、层次及取舍理由；不得恢复人为综合评分、因子或组件权重、风险扣分、置信度分和推荐分数门槛。
- 必须说明当前样本、来源失败与技术/完整报告覆盖，只比较本次实际取得的候选。范围整体下跌时仍能比较相对最优，并明示绝对表现偏弱；用户要求上涨时才将其作为限制。无合格候选时可保留最多一个已深度复核的观察对象，明确未通过条件，不称首选或备选，不为凑数制造结果。
- 用户明确观点通过 `yanjiu_wenti` 保留原文、忠实的可观测命题与不能核验的部分。程序返回支持、部分支持、直接反证或证据不足及逐项原值；不能用任意代理指标声称验证整个口语观点。无观点时直接综合分析。财务正负不机械加入看涨/看跌票数；同源价格事实归组，缺口与复评条件保留。
- 同行优先真实行业一致、同日已核验总市值相近者；未核验规模只能作为明确披露的补位参照，不声称规模匹配。行业标签不能代替主营业务核验；比较池仍有界，不能称作完整行业或全市场统计。
- 全部范围候选核验通过但名称匹配未唯一确定时，先允许一次智能体语义审查；选择必须引用本次候选并保留解释，原始范围、数量和市值限制不变。未核验候选不得被忽略，未完成审查不得包装成成功；目录仅在同一请求两步间复用。

## 数据与分析边界

- 市场数据现取现用，只允许一次请求内的内存复用；行情、目录、成分、因子和分析输入不得持久化为本地市场数据缓存。
- 三交易日预测、模型训练、预测工具、预测资格和确认流程已经移除，不得通过提示词或结果字段重新引入。独立 MACD 历史回放只做确定性研究，不训练或发布预测，不自动改变生产筛选规则。
- 跨进程需要新的市场判断时，重新获取远端数据并分析。旧预测阶段、旧评分结果或失效会话不能冒充当前分析。
- 正式日线与盘中快照分开，明确实际截止日和生成时间。交易日身份只依赖权威日历，不以星期近似。每个行情来源采用相同的对应请求质量检查，不合格时才在受限范围内使用备用源，不拼接不同来源复权价格。
- 实时价量字段齐全不等于行情已核验。必须保留逐股来源日期、更新时间与取得时间，核对待分析交易日并拒绝未来时点；缺失或过期时保留原始快照，但不参与当前成交、盘中形态或尾盘确认。收盘前报价不能冒充完整收盘。更新时间只用于说明时点与延迟，不代表可保证即时成交。
- 当前快照使用120秒有效交易时段延迟核验，竞价后等待、午休与收盘后不积累延迟；该值是保守工程口径，不是行情或成交保证。当前可交易性需有效正成交量与成交额，09:25撮合前不确认成交；历史日线不强制当前时效，盘中金额不套用全天流动性底线。
- 非实时分析的历史基础成交条件与当前行情核验分开；休市、盘前和盘后不能因历史快照字段齐全而宣称当前可交易。历史参考价采用最近完整日线，保留快照原值与核验范围。
- 日终横截面补充换手率、流通市值时，逐行核验明确日终来源、行情日、估值日与目标日线日，已有有效历史值优先。宽基指数复用本次权威交易日历，当前21日窗口质量不合格时切换整段来源，历史缺口不得跨越计算收益。
- 概念范围不能补成行业；只有已核验的行业范围可补充缺失行业。动态市盈率、滚动市盈率和来源其他口径分开保存，未明确口径不得混入滚动市盈率的相对比较。
- 指标中文定义由统一注册表提供，展示层不得复制整套指标字典或重算数值。真实历史换手缺失时不以成交额相关性冒充；行业统计使用真实标签和交易日，比较池广度不能称作全市场广度。
- 展示时依据字段所在结构区分指标值与来源元数据；`feature_coverage` 是有效观测覆盖率，不套用同名指标的收益、波动率或分位单位，覆盖率为零不等于指标为零。
- 下行风险、ATR 偏离、CMF、流动性稳定性、增长、现金流和财报时点统一消费本次数据，明确样本与缺失。CMF 不是实际主力资金流入；现金流单位未核验时只解释符号。EMA、ATR、CMF 公式中的数学权重仍正常保留，不等于人为综合评分。详细口径见 `量化指标说明.md`。

## 工程质量

- 数据适配与重试、范围策略、纯计算、业务编排和对话展示分层。优先普通函数、小型数据类与组合，避免平台化设计和没有实际收益的抽象。
- 环境与配置路径使用 `core/config.py`，数据层不因读取凭据导入模型 SDK；默认 `agent/src/lianghua_peizhi.json` 随包发布，根目录同名显式配置保持兼容。历史研究参数由轻量共享配置定义校验，不为配置校验加载回放链路。项目测试源码与测试依赖已按用户要求移出，发布包排除测试、虚拟环境、运行记录和字节码。
- 状态区分分析完成、部分证据缺失、没有合格候选、需要澄清、必要数据不可用和程序错误。外层继承业务状态，不能因模型写出文字就把 `partial`、`unavailable` 或 `error` 包装成完整成功。
- 范围的 `selection_outcome` 与报告完整状态分开：有候选仍可为部分报告，必需证据不足不能说成已确认全部不合格。范围分批全量请求；完整比较、核验潜在合格者后，再为最终候选补财务与分钟数据。完整报告和分钟请求各守上限；展示数量不改写技术复核数或全部已核验合格数。程序错误终止，不能用后续股票掩盖。
- 共用可交易性和指标计算，范围复核后根据当前证据重新评估显式条件；单股保留限制供解释。不能累积已失效风险，也不能通过展示改写状态。
- 指标缺失不填中性值，成交额不以收盘价乘成交量替代；EMA、RSI、ATR遇缺失重新预热，主技术与补充诊断共用ATR。注册表公开公式、窗口、最低样本与缺失规则。复核核对股票、日期和未舍入数值；历史失效形态及参考价只保留在历史组件。
- 对话压缩不能丢失本轮实际完成状态、候选和关键反证；从结构化结果补齐完整报告。同一请求的完全相同调用可复用内存完成结果，下一轮必须释放；共享指标定义不得丢失逐股有效样本数。
- 单股与返回候选使用共享完整报告。模型遗漏时按结构化组件补齐，保留缺失原因且避免重复；不用旧买卖标签或分数关键词作为成功门槛。
- 新增关键来源、指标或规则时同步长期文档。新增指标包含原始值、中文定义、单位、窗口和缺失规则；新增范围条件明确真实阈值，不能借覆盖率或命中数量暗中恢复评分。
- 验证仅覆盖修改的直接影响范围。临时脚本、日志和中间产物交付前按用户规则移入垃圾桶，不永久删除，不创建无要求的备份；不得自行修改项目或契约版本号。
