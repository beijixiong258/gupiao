# 股票诊断指标口径

本项目用八组原始指标解释股票状态，在用户明确条件内优选已有技术表现。单股分析不生成综合评分或强制买卖结论；范围选股逐项核验数据及用户条件，再按20日、5日已实现收益作非支配分层，同层明确优先20日表现，不使用因子权重、风险扣分或置信度分。

指标数量以当前统一注册表为准，不把固定数量写成能力承诺。实际可用内容取决于本次行情、历史窗口、比较样本和财务来源；每组公开原始值、中文定义、可用字段数与缺失字段，不把缺失当零。

## 八组原始指标

| 组别 | 主要内容 |
|---|---|
| 趋势结构 | 均线间距、20 日对数价格日均变化、趋势变化与波动比、短中期均线关系 |
| 动量与反转 | 多周期收益、RSI、MACD DIF 与柱体 |
| K 线压力 | 跳空、日内收益、收盘位置、实体与上下影线 |
| 价量确认 | 量比、成交额异常、价量相关与量价残差 |
| 突破与回撤质量 | 突破距离、放量、回撤与停滞 |
| 相对强弱 | 本次样本、真实同行和宽基指数的相对表现 |
| 风险与流动性 | ATR、波动、振幅、回撤、真实成交额与换手率 |
| 市场背景 | 比较样本和行业收益、广度、离散度与宽基背景 |

`yinzi_gongcheng.py` 的 `FACTOR_REGISTRY` 与 `factor_definition` 统一提供中文名称、单位、含义、显示倍数、公式、窗口、最低有效样本要求、缺失规则和来源口径。输出 `daily_factor_analysis.groups` 中每组含 `values`、`metric_definitions`、`missing_fields`、`available_factor_count` 和 `factor_count`。因子汇总保留股票代码和实际交易日期。

指标定义同时传递同源标识、输入要求、日线频率和可用时点，供 AI 识别同义字段及适用边界。收益与收益排名、同一个超额的兼容别名不能当作独立上涨确认。市场方向分歧只表示指数与本次比较池未形成同向强弱，不等于本股横盘。

允许不足20条的滚动统计额外保留各股本次实际有效样本数 `valid_observations`，包括零样本。工具消息将重复定义提取到顶层 `indicator_definitions`，逐股用 `definition_ref` 引用并保留各自样本数；展示层从同一注册表展开，完整数值、定义和缺失项不减少。

展示层消费这份定义。例如原始收益 `0.05` 按定义显示为 `5%`；量比显示倍数，金额显示元；RSI 仍保留其自身的 0–100 指标尺度。上述都是指标的数学单位，不能被改称为综合分或上涨概率。可用字段数只是报告覆盖情况，不会换算成置信度分。

来源记录中的 `feature_coverage` 表示该字段有效观测占比，独立展示为覆盖率。例如 `0.9687` 只能表示覆盖率 `96.87%`，不能套用同名指标解释成收益或波动率；覆盖率为零不表示换手率、收益等原始指标为零。MACD 的陈旧信号与行情报价过期也分别解释。

没有组内复合分或市场状态综合分。原始指标与它的横截面分位可同时存在，但不被累加；原始值、排名和同义字段不再存在重复计权问题。数学定义中的指数平滑、成交量或成交额加权仍按公式保留，不属于人为设计的选股综合评分。

## 原始数据与比较口径

- 核心收益和均线要求完整连续窗口：N日收益需要N+1条有效收盘价；均线需要N条。坏行不删除后拼接，重复或无效日期拒绝计算。真实交易日是否缺口由来源层用权威日历核验。
- EMA、RSI、ATR 沿用首个有效值作种子的 `adjust=False` 递推口径，遇缺失后重新预热。RSI 的涨跌幅以首个有效差分作种子；本程序没有改为“首14期简单均值作种子”，短历史时与采用该口径的软件可能有差异。MACD 使用 EMA12、EMA26、DIF的EMA9，柱值为 `2*(DIF-DEA)`；完整柱值至少需34条连续收盘价。递推定义见 [pandas 官方文档](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.ewm.html)，常用参数与TR定义参考 [MACD](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/technical-indicator-guide/macd) 和 [ATR](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/technical-indicator-guide/atr)。实际结果以本程序公开的初始化、倍数和缺失规则为准。
- `position_20` 历史不足或价格范围有缺失时留空，只有完整20日全平区间约定为50%。日内高低价相等时收盘位置留空；缺失开盘价不能用收盘价替代计算影线。横截面与单股复用这些口径。
- 成交额相关指标仅消费真实非负 `amount_yuan`；禁止用收盘价乘成交量补值，负额不能截成零。20日均额需20条有效值；成交额异常与相关系数使用最近20条中的至少10条/对有效观测，窗口与最低样本不是同一概念。
- `wvma_20` 的每日报酬权重为真实成交额相对20条窗口中位数之比，并截在0.25至4；基准至少10条金额，最终波幅至少10对有效收益和权重。分子、分母使用完全相同的配对。此数学权重不参与选股综合打分。
- `volume_price_residual_20` 使用带截距的一元线性拟合，收益与成交额对数变化至少10对有效观测；均值、协方差、方差使用同一配对，输出当前残差，不作未来预测。
- 前20日高点距离严格使用当日前20条完整有效高低收盘，不以10条替代20条。突破与均额比的乘积不能当作独立确认。旧字段 `low_volume_long_lower_shadow` 实际使用均额比，公开名称及说明已明确这一点。

- `price_turnover_corr_20` 使用真实逐日换手率，计算日收益与换手率日变化的 20 日相关，至少需要 10 对有效观测。输入 `turnover_rate=5` 表示 5%；缺失或常量时不可用，不能复制成交额相关系数。单日快照不能冒充历史换手序列。
- `wvma_20` 是成交额加权日收益均方根，描述无方向波动；数值高不能直接解释成更有利的股票状态。
- 行业统计按真实标签和交易日分组，每项至少五个有效观测。单股真实同行不足时记录缺失，不把整个比较池称为行业。
- 概念范围成员的行业缺失时保持缺失；只有已核验的行业目录范围可补充对应行业名称，已有成员行业不被覆盖。
- 比较池广度只代表本次取得的样本。当前行业标签回看历史不等于历史成员重建；沪深 300 等宽基指数单独作为市场参照。
- 基本面保留本次全部可用字段、日期、来源和缺失原因，不只截取前几条。市盈率、市净率的相对估值包含原始值、0–100 的百分位、样本数、比较来源与原因；分位是样本位置，不是质量评级或收益概率。
- 只有原始 PE/PB、估值分位不可用或有效比较样本不足时，不据此断言估值偏高或偏低。有可比分位时也只描述明确样本、日期和口径下的位置，不直接等同内在价值高估或低估。
- 相对估值只使用与目标同日、逐行核验为完整日终来源的正估值样本；目标自身时点未核验时不发布分位。错日、来源不明的原值继续展示，并注明不可比较原因及排除数量。
- 估值字段严格分口径：[AKShare 东方财富现货接口](https://akshare.akfamily.xyz/data/stock/stock.html)的“市盈率-动态”存为 `pe_dynamic`；[Tushare 每日指标](https://tushare.pro/document/2?doc_id=32)的 `pe_ttm` 是滚动口径，`pe` 按来源的“总市值/净利润”定义保留，不擅自改名为动态口径。未明确口径的 `per` 保留为 `pe_unspecified`，不进入滚动市盈率比较。
- 有界样本、公开接口缺失和时点差异会限制解释。单股保留可用信息并显示 `partial`；选股所需条件缺字段时不能视为通过。

## 行情时点与解释约束

东方财富快照和成分接口读取逐股 `f124` 更新时间（Unix 秒），转换为北京时间，同时保留来源日期；一次小样本直连核验确认了该字段的响应，正式调用仍检查缺失和无效值。取得时间不能代替来源时间。来源日期与待核验交易日不一致、来源时间在未来，或盘中缺少来源时间时，当前成交核验不通过，已有完整日线仍可用于诊断。报价更新到核验时点的秒数单独显示，不捏造固定有效期或成交保证。收盘前报价不能用作收盘确认。

单股和范围候选复用证据概览，明确价格与短中期均线、量能、相对表现、市场背景、下行风险、形态适用状态及真实分歧。复评条件来自当前有效 MACD 信号和适用形态的已计算条件；失效或未形成信号的原始状态仍可查阅，不继续支撑当前判断。不适用形态不等于利空，完整历史不等于已经验证的上涨胜率。

涨停回马枪的 `rule_semantics` 明确交易日计数起点与配置期限：基准涨停日为 D0，之后第一个交易日为 D1；默认缩量窗口为 D1～D7，突破必须晚于缩量且不晚于同一基准的 D14（含 D14）。缩量不重新起算14日。实际配置、缩量及突破距基准的交易日序号一并返回，不推断未来日历日期。

内部设问和推演不作为报告内容。公开提供关键证据、反证与限制，完整指标继续保留；不把每个价格衍生指标机械解释成一个独立买卖信号。

## 七类补充诊断

| 诊断块 | 可复现口径 | 能回答的问题与限制 |
|---|---|---|
| 下行风险 | 20 个日收益的下行半偏差：`sqrt(mean(min(r, 0)^2) × 252)`；60 条收盘价的最大回撤：`max(1 - close / cummax(close))` | 区分下跌波动与总体波动；回撤保留已修复的历史跌幅。年化是尺度换算，不是未来损失预测。 |
| 均线偏离尺度 | `(最新收盘价 - MA20) / ATR14`，ATR 沿用 `ewm(alpha=1/14, adjust=False, min_periods=14)` | 描述价格偏离了几个历史波幅，不直接推断反转。价格整体缩放不改变倍数。 |
| 持续价量压力 | 20 日 `sum(((2C-H-L)/(H-L)) × V) / sum(V)` | 收盘位置加权成交量（CMF），不是实际账户资金净流入或主力动向。项目明确采用 20 日窗口。 |
| 流动性稳定性 | 20 日真实成交额中位数、低于 `fenxi.min_amount_yuan` 的交易日数量 | 区分稳定成交与偶发放量。不以价格乘成交量补金额，不取代当前可交易性检查。 |
| 营收与利润增长 | 同一期报表营业收入同比、净利润同比及正负零组合 | 说明两者是否同步；单期同比不能证明持续性或一次性损益原因。 |
| 盈利与现金流核对 | 净利率和已有经营现金流指标的符号交叉核对 | 指出盈利与现金流符号冲突。现金流字段单位未确认时保留原值，只解释符号，不擅自加百分号或计算现金利润比。金融类公司需结合业务结构解释。 |
| 财报时点与来源 | 报告期、公告日、已知截止日、来源及与分析日的自然日间隔 | 日期或来源缺失不能称已核验；晚于分析日或日期矛盾的字段不能用于当期补充解释。不设置任意财报年龄扣分。 |

计算只使用本次已有数据，不额外请求远端来源，不训练、不发布预测，也不持久化行情。

## 缺失、窗口与时点

- 不缩短窗口冒充完整指标。20 个日收益需要连续 21 条有效收盘价，60 日回撤需要 60 条有效收盘价；不跨越缺失日拼接收益。
- 最高价等于最低价时，日内压力无法辨认，CMF 按零贡献处理并明确部分不可辨认；整个窗口无价差或无成交量时不发布 CMF。ATR 为零时不计算偏离倍数。
- ATR 遇到坏行时，只使用之后连续有效日线，先截段再计算前收盘价，避免坏行污染新段首真实波幅，并记录使用行数。
- 未来日线先排除；重复或无效交易日期保留不可计算原因，不随意挑选坏行。结果分别记录请求截止日与实际最新日线日期。
- 零值、缺失、样本不足、不适用是不同状态。某项缺失只限制对应解释，不把缺失风险伪装成零。
- 正式日线与盘中快照分别显示，盘中累计金额不冒充完整交易日成交额，动态均线不覆盖正式收盘均线。
- 横截面换手率和流通市值只允许经核验的同日日终值填补日线缺失：当前需 `tushare_daily_basic` 来源、完整日终标记，且行情日期、估值日期与目标日线日期逐行一致。已有有效历史值优先，实时值或未知时点值独立保留。
- 单股基本面与比较池按交易日复用同次请求内的日终估值响应，按证券代码和日期核验后使用；单股 `turnover_rate_pct` 按百分数口径衔接 `turnover_rate`，再由原因子公式归一化。已核验目标估值不被比较池空字段或未核验快照覆盖，不新增本地行情缓存。
- 沪深300等指数采用本次权威交易日历核验截止日与末21个交易日。必需窗口缺日、重复日期或收盘价非正/非有限时拒绝该源，按既有顺序尝试备用源，不拼接不同来源价格。历史收益按交易日历重索引，窗口内任一缺口均不计算该窗口收益；来源失败和降级保留结构化记录。
- 备用指数通过同样质量检查后，主源失败保留在尝试记录及 `source_warnings` 中，不再列为仍需补齐的数据；所有来源均不可用时继续记录真实缺口。
- 当前行情有效交易时段延迟上限为120秒，扣除09:25–09:30、11:30–13:00及收盘后停更时间，并单独记录实际墙上延迟。15:00后拒绝未到收盘的报价；09:25撮合前不确认成交，当前可交易性需正的有效成交量与成交额。该上限是工程核验口径，不是交易所规定或成交保证；非实时历史核验不套用当前时效。
- 非实时分析返回 `execution_check_scope=historical_daily`，基础条件通过时为 `historical_reference`；历史快照核验与 `current_quote_verified` 分开，当前核验状态为 `not_required`，参考价取完整日线。实时条件通过为 `conditions_verified`，同样不保证即时成交；时段来自权威市场时钟，不以星期猜测。

## 接入与展示

`buchong_zhenduan.py` 提供共用技术补充入口，`caiwu_zhenduan.py` 提供财务解释。单股和范围深度复核调用相同函数。`supplemental_diagnostics` 包含整体状态、数据时点及七个块；每块含名称、状态、规范摘要、原始指标和缺失原因。

补充块直接保留诊断事实，评分用途字段已移除。补充证据不产生额外选股门槛。缺失原因进入 `evidence_gaps` 与复评条件，不能将缺失财务或补充块改写成凭空的买卖判断。

单股与每个返回的研究候选都展示完整组件；没有合格候选但存在已深度复核对象时，最多保留一个“观察对象（未通过选股条件）”。观察对象不能被解释成推荐。早期没有可分析数据时不会制造对象。

模型遗漏证据时，展示层按股票补齐规范内容，不重算数值，也不因漏掉一个块把整篇回答换成空泛错误。八组指标、基本面、技术、形态、尾盘、补充证据和来源都能查看，缺失原因与零值同样保留。

## 范围选股的条件与分层

本程序在用户明确范围与条件内优选已有技术表现。默认不采用ST、上市时长、成交额大小或低波动偏好；数据有效性与适用的成交核验继续保留。

用户提出市值限制时，必须先明确口径（总市值或流通市值）、上限（输入单位亿元）及比较边界（低于 `<`，不超过 `<=`），不设“小盘股”的隐含默认值。市值是候选池条件，不参与表现排序或评分；在基础价量检查后、分批历史行情请求前执行。当前只支持绝对上限，市值百分位请求需澄清，不能暗中换成抽样百分位。

筛选使用分析日的 `tushare_daily_basic.total_mv / circ_mv`，来源单位万元乘以10000转为元；上限亿元乘以100000000转为元。逐股核对来源代码和日期，值须有限且为正；同股同日冲突、日期缺失或错日、接口失败均记为 `unavailable`，不通过条件，也不计作 `unmet`。盘中或未核验快照保留为原始资料，不能替代这项日终筛选。公开 `market_cap_filter` 的汇总和返回股票的 `market_cap_check`（条件、原值、单位、来源、日期、取得时间）；存在未核验市值时，报告保留 `partial / information_partial`，即使其余候选已通过或已确认未通过其他条件。

范围默认要求有效价量、完整分析日、20日收益的21条连续有效价格及适用的成交核验。显式用户条件经 `tiaojian` 单独返回指标、比较关系、实际值和界限；缺失为 `unavailable`，不当作通过。此前全部上涨、放量、波动上限等不再默认同时要求。

合格候选以20日、5日已实现收益越高越优作二维Pareto非支配分层；同层先20日、再5日降序。两项完全相同为并列，代码只稳定显示。保留与后一候选的原值、层次和取舍理由。该规则是技术表现优选口径，不是经验证的未来收益能力或公司价值最优；范围整体下跌时仍可比较相对表现，并明确绝对弱势。

两个窗口相关，不能视为独立确认。同一交易日同一基准下，20日超额与20日收益仅相差一个常数，不再重复参与排序。波动和金额保留原始观察，不默认决定优劣；均线、MACD、量价与当前形态用于解释和用户明确条件，不武断假定越大越好。

深度技术复核使用 `raw_indicators` 的未舍入值重新核验本股条件，同时核对股票身份、因子日期、技术日期和目标日线日期。初筛与复核数值冲突或必需值缺失时标为证据不足，不让初筛旧值通过；MACD展示舍入不改变正负判断。失效形态及旧风险参考价只保留在历史形态组件，不追加到当前风险或当前参考价。

范围名称匹配不唯一但候选全部核验通过时，工具先返回内部语义审查状态。智能体可根据完整请求选择其中一个真实候选并保留解释；原始范围、数量、市值及其他用户条件不能改变。目录只在同一请求的两步之间内存复用，请求结束或下轮开始即释放。仍有歧义时只澄清一次，核验失败不得被当成排除候选的依据。

同一请求中重复调用完全相同的已完成分析时复用该次内存结果；下轮请求清空，再取远端数据。对话压缩后以程序保留的完成状态、候选、原始值、反证和缺口继续解释，不能让摘要遗漏把已完成分析重新变成范围澄清。完整报告始终从结构化结果补齐。

范围分批请求全部通过必要基础检查与用户市值条件的成员；先逐股计算完整窗口，再合并末日截面，广度和行业统计按全部可用候选计算。潜在合格者完成技术核验、重新比较后，才裁剪展示并请求少量财务和分钟数据。来源缺口和实际阶段数量公开，不能把请求过等同于数据已经核验完成。

允许的局部日线缺口按权威交易日历插入缺失行，不填价格或成交量；收益窗口及EMA预热不得跨越缺口。已取得指标的未舍入原值用于核验。可选MACD/长窗口缺失本身不否定已完整的20日表现，但依赖该指标的用户条件仍不能通过。

单股观点以问题原文、可观测命题和不能核验部分组成结构；程序逐项比较注册表原值，只允许同单位、同日期参照直接比较。财务 `_pct` 保留百分数口径，收益和波动保留小数比例。财务为正仅作经营背景，不能单独确认价格强势；同源价格别名及变换归组，不按指标数量投票。当前形态与历史失效状态分开，复评条件引用真实指标和已计算参考值。

盘中补充使用已核验快照的现价/前收盘价变化，只有前收盘与历史前复权收盘口径对齐（金额差不超过0.005元）才与已完成日线MA20比较。该均线是历史观察参考，不是当日最终均线；盘中变化待收盘复核，不替换完整日线的比较顺序。

## 公式与字段参考

- [Fidelity：Chaikin Money Flow](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/technical-indicator-guide/cmf)：收盘位置加权成交量公式。其常见窗口为 21 日，本项目明确采用 20 日观察窗口。
- [Fidelity：Average True Range](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/technical-indicator-guide/atr)：真实波幅与 ATR 的非方向性；本项目沿用已有指数平滑初始化方式。
- [Tushare：财务指标接口](https://tushare.pro/document/2?doc_id=79)：营收、利润率及 `ocf_to_or` 字段含义。该字段描述为经营现金流净额与营业收入之比，当前未确认数值单位，因此补充诊断只解释符号。

这些来源用于说明定义；公式、数据边界和展示测试只验证实现，不证明指标或筛选条件具有已验证的投资收益能力。
