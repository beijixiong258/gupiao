# 程序介绍：A 股统一选股分析与三交易日预测

## 1. 程序定位

本程序是个人使用的中国大陆 A 股研究工具。量化分析是默认主流程，预测是高成本、需二次确认的可选流程：

1. **分析**：可以量化评估一只明确股票，也可以从全市场或用户用日常语言描述的范围中筛选股票；前者复用统一评分门槛并明确给出“建议买入/暂不建议买入”，后者给出一只首选、最多四只备选，或者明确说明当前没有合格候选。
2. **预测**：范围选股出现合格候选后程序主动询问是否继续；只有用户明确确认，才重新下载远端数据、训练既有模型，并一次返回 T+1、T+2、T+3。单股买入结论不进入预测。

用户始终通过自然语言使用程序，例如“帮我选股票”“从半导体行业里选”“预测刚才的第二只备选”。工具名、结构化参数、分析编号、内部分析模式和流水线步骤都不会要求用户理解或传递。

程序不连接证券账户、不提交委托、不自动交易。单股的买入标签是按固定门槛生成的个人量化研究建议，综合分不是上涨概率；它不构成证券投资咨询、交易指令、目标价或收益承诺。

## 2. 分析功能

### 2.1 支持的范围

面向用户有三种自然表达：点名一只股票时做单股量化并回答是否建议买入；不限定范围时分析当前可取得的全部 A 股；限定范围时直接使用“电子板块”“消费电子”“AI 算力”等日常说法。

单股量化不会把股票名称当成板块名称，也不会在数据源异常时改成全市场选股。它读取目标股票完整日线、一次实时快照、当前横截面、一个同行优先且由市场参照补足的有界比较池，以及截至分析日可核验的基本面；然后直接复用统一选股的八组日 K 因子、基本面、形态、尾盘、风险扣分、综合分和推荐门槛。最终结果固定落到“建议买入”或“暂不建议买入”，并列出全部未通过条件。结尾还会按已经计算的证据生成一段口语化收束总结，说明股票好在哪里、为什么当前买或不买，以及满足哪些条件后值得重新评估。回答必须展示综合分、证据完整度、八组因子分、5/10/20 日动量、组件分、风险扣分，以及价格均线、MACD 正反证、波动回撤、基本面来源失败原因、成交额日期口径和日线覆盖质量；缺失关键理由、收束总结或把完整交易日成交额说成盘中成交额时，展示边界会按结构化事实重建答案。该建议属于公开数据量化研究，不是收益承诺或自动交易指令；单股结果仍不产生预测确认窗口。

程序不会维护一份容易过期的板块枚举，也不会在访问数据源前强迫大模型猜测“行业还是概念”。它会实时读取东方财富当前的行业和概念目录，生成真实候选，并用官方详情页再次核对名称、类别和范围身份。唯一且已经核验的精确候选可直接采用；如果存在多个真实候选且智能体结合上下文仍无法判断，才用普通中文澄清一次。目录不可用或核验冲突时会明确说明数据源问题，不会回答“没有这个板块”，也不会把专业分类责任推给用户。

### 2.2 自动分析流程

一次分析会自动完成以下工作：

1. 对指定范围动态取得行业与概念目录、完成事实核验，再取得真实交易日历、最近完整收盘日、横截面和必要的实时行情；
2. 建立候选池，执行代码规则、上市天数、ST/退市风险、新股、停牌、无效价量、一字涨停、流动性和历史完整性等硬过滤；
3. 先按行业内与全市场流动性进行分散预筛，避免候选池被单一行业或极端成交额股票占满；
4. 批量读取复权日线，计算八组日 K 因子与横截面相对位置；
5. 加入估值、基本面、同行和行业证据；
6. 运行“涨停回马枪”状态机；
7. 14:30 后自动加入尾盘证据，并只为少量靠前候选读取 5 分钟数据；
8. 对最终靠前的少量候选执行深度基本面和技术复核，其中包含指数平滑异同移动平均线结构；
9. 汇总有效证据、扣除独立风险分，输出首选、备选或不推荐结论。

智能体负责理解意图、审查范围歧义和来源质量、解释程序证据并决定澄清或拒绝；量化程序负责可复现的行情处理、硬过滤、因子、形态、评分和门槛。用户不需要选择“技术分析”“尾盘分析”或“涨停回马枪”等模式，智能体也不能改写程序算出的数值或绕过风险规则。

### 2.3 八组日 K 因子

八组因子仍是分析主体：

| 因子组 | 主要观察内容 |
|---|---|
| 趋势结构 | 均线结构、趋势斜率、趋势拟合质量、MA5 与 MA20 相对间距变化 |
| 动量与反转 | 多周期收益、RSI、MACD DIF 与柱体 |
| K 线压力 | 跳空、日内收益、收盘位置、实体和上下影线 |
| 价量确认 | 量比、成交额异常、价量相关和量价残差 |
| 突破与回撤质量 | 突破距离、放量确认、回撤质量和停滞压力 |
| 相对强弱 | 相对同行、行业、全市场和宽基指数的表现 |
| 风险与流动性 | ATR、波动率、振幅、回撤、成交额和换手率 |
| 市场背景 | 市场/行业收益、上涨广度、离散度和市场状态 |

各组先把不同方向的指标统一为“越高越有利”的证据方向，再按同日横截面位置汇总，避免直接平均含义相反的原始数值。证据缺失时从有效权重中移除，同时降低可信度；缺失不会被伪装成条件失败。

### 2.4 指数平滑异同移动平均线结构研判

程序在统一日线特征入口一次计算快线、慢线、柱体，以及按收盘价标准化的慢线、两线间距、变化速度和零轴相对距离。独立结构模块只接收这些特征，不访问网络、不保存行情，也不重新计算另一套指标口径。

结构结果包含：

- 快慢线同在零轴上方、同在零轴下方或分处两侧的趋势背景；
- 最近一次金叉或死叉的实际交易日、所在区域、标准化强度、信号年龄、新鲜度与失效条件；
- 最近三日柱体属于动能增强、衰减还是方向混杂；
- 价格相对快线、价格相对柱体的顶部和底部背离，四类信号分别展示；
- 两个价格拐点、两个指标拐点、真实确认日、变化强度、证据可靠性、有效期和失效原因；
- 支持证据、反证、风险提示以及“不是上涨概率”的用途说明。

默认拐点方法使用左侧二个、右侧二个交易日确认，只有右侧数据真实出现后才发布信号；背离还需满足最小五个交易日间隔、2% 价格变化和相对收盘价 0.02% 的指标变化。零轴附近、变化幅度和失效容差全部使用相对尺度，不使用固定价格单位。具体参数集中在 `lianghua_peizhi.json` 的 `fenxi.macd_structure`，并由配置入口校验。

这一能力目前只提供解释性证据，不增加因子字段数量、不改变综合排名，也不进入预测模型。顶部背离只进入风险文字，底部背离只说明下跌动能减弱；金叉、死叉、背离或零轴状态都不能单独触发推荐。智能体会把这些结构与大盘、当前范围、相对强弱、价量和流动性证据一起解释，并明确相互冲突之处。历史表现未验证出稳定增益前，不启用结构子分或顶部风险扣分。

研究回放入口只接收本次调用内存中的历史面板，不自行下载或保存市场数据。它使用实际确认日，按次一市场交易日开盘到指定未来收盘计算成本后收益，并记录停牌、封板、整手、成交额参与率、滑点和成本造成的不可执行样本。报告同时给出同日无结构基线、时间子区间、大盘、行业、相对强弱、价量、股票范围和附近参数的稳定性；生产基线、历史行业成员时点核验或关键覆盖不足时，会返回未满足项而不是宣布验证通过。相关固定口径位于 `fenxi.macd_structure_validation`，回放本身永远不自动修改生产评分。

### 2.5 涨停回马枪状态机

主板 10% 涨跌幅股票会按明确状态评估：

- 调整中；
- 极致缩量；
- 等待突破；
- 盘中暂定确认；
- 收盘确认；
- 失效；
- 过期。

状态机使用合法涨停价、价格容差、D1–D7 调整区间、缩量比例、突破期限和放量条件。盘中突破与完整收盘确认严格分开，旧的历史突破不会继续给当前排名加分。ST、新股和非主板规则股票不会套用主板 10% 模板。

### 2.6 尾盘证据

尾盘阶段按北京时间和真实交易日历判断：

| 时间 | 状态 |
|---|---|
| 14:30 前 | 不适用，不加入尾盘分 |
| 14:30–14:44 | 尾盘初筛 |
| 14:45–15:00 | 盘中暂定，加入分钟走势、最高价时间、回撤和 VWAP |
| 15:00–15:05 | 等待完整收盘，不发布伪收盘结论 |
| 15:05 后 | 数据源已有完整日线时进行收盘复核 |

动态 MA5/MA10 使用前一完整交易日以前的收盘序列加当前价格计算，不会覆盖日线因子中的正式收盘均线。缺少分钟数据时仍可使用已有证据，但可信度会下降。

### 2.7 排名与结果

默认综合分由以下部分组成：

- 八组日 K 因子：65%；
- 基本面与估值：15%；
- 涨停回马枪形态：10%；
- 尾盘证据：10%；
- 最后单独扣除风险分。

某一部分在当前时点不适用或数据不可得时，程序只在有效部分之间重新归一化权重，并降低总体可信度。综合分不等于上涨概率，也不会被换算成概率。

每只入选股票会提供：

- 股票代码、名称、所属行业和分析时点；
- 综合分、可信度和各组成部分得分；
- 八组因子证据、基本面证据、指数平滑异同移动平均线结构、形态状态和尾盘状态；
- 风险扣分、命中条件、未满足条件、不可用证据和数据来源；
- 供人工复核的技术与基本面摘要。

只有同时达到最低综合分和最低可信度的股票才会进入推荐结果。没有合格候选时，程序明确返回“不推荐”，不会为了凑数强行给出股票。

## 3. 预测功能

预测只能衔接上一轮分析中的合格候选。程序不会因为候选达到门槛就自动预测；它会先用自然语言讲清量化结论、关键依据和主要风险，再说明预测需要重新下载数据和训练模型。连续对话会等完整分析答案显示后再弹出编号窗口：最多列出首选、靠前备选和“暂不预测”四个预设项，并允许直接输入其他合格备选的名称或代码。即使用户在最初的问题中说“分析完直接预测”或“不用再问”，程序仍要求完成这次选择。该选择作为新的用户回合，不会再做第三次确认。没有合格候选时不显示预测窗口。

日常范围名称确实存在多种真实解释时，程序也会复用同一窗口提供二至四个通俗选项；能从数据源和上下文合理确定时直接继续，不为数据源、指标模式或其它内部实现细节打断用户。非交互式单轮命令和 MCP 不弹出终端窗口，而是退化为一次简短文字询问。

预测算法的业务口径保持不变：

- T 为最近一个已确认的完整收盘日；
- 一次固定返回 T+1、T+2、T+3；
- 目标日期来自真实交易日历，不用自然日代替；
- 模型在目标股票与流动性同行的历史面板上训练；
- 目标日线、同行历史、日频估值和宽基指数都在确认预测后从远端重新获取，不读取或写入本地市场数据缓存；
- 保留既有特征工程、滚动样本外验证、方向概率校准、收益估计和区间估计；
- 分析排名分不参与概率换算，也不改变预测标签。

每个期限会返回实际目标交易日、方向、上涨/下跌概率、累计收益估计、参考收盘价、参考区间、验证状态和可信度。数据时点、同行样本或训练条件不满足时，程序会说明不可用原因，不补造日期、概率或价格。

## 4. 数据时点与性能

一次分析建立一个请求级数据上下文，仅在当前调用内临时复用远端响应：

- 交易日历与市场阶段；
- 最近完整收盘日；
- 全市场实时快照，单次请求最多读取一次；
- 全市场日线与估值横截面；
- 行业/板块成分；
- 批量复权历史日线；
- 少量候选的分钟线。

股票资料、日线、估值、宽基指数和同行样本都从本次远端请求取得，不落入本地行情文件或 SQLite 仓库。指定范围的目录、详情核验和成分来自东方财富当前公开接口；分析所需前复权日线以腾讯当前公开接口为主，并用同一响应中的证券快照核验身份及最新价、量、额，东方财富和 Tushare 只做受控备用。交易日历优先使用 Tushare，限频或暂时不可用时使用新浪交易所日历；所有权威日历均不可用时停止依赖交易日身份的判断，不用“周一到周五”冒充法定交易日。

公开数据 HTTP 边界统一处理超时、限频、短暂断连、结构漂移和重试记录。Windows 下可明确选择直连或继承系统代理；直连模式不会从环境或系统设置中偷偷使用代理。分析结果结构化区分“推荐成功、没有合格候选、需要澄清、远端数据不可用、程序错误”，远端不可用不会再被外层显示成业务成功。

统一分析只读取满足因子计算的较短批量窗口。分析展示完毕并得到用户明确确认后，程序才重新为目标候选和同行下载预测配置中的完整长历史窗口，避免全市场分析承担模型训练成本，也避免短分析窗口改变既有预测行为。

数据源、分析日期、复权方式、缺失字段、降级原因和盘中暂定状态都会进入结果，旧数据不会被描述为当前完整行情。

本地配置、凭据、对话文字和运行记录与市场缓存分开治理。默认运行记录只保存状态、耗时等元数据，自动受 14 天、100 次、100 MB 三重上限约束，并可用 `/clear-runs` 清除；持久会话只保存文字和“需要重新分析”的最小标记。行情、目录、成分、因子和预测输入不持久化，跨进程继续预测时会重新分析，而不是从旧记录恢复市场事实。

## 5. 使用方式

进入连续对话：

~~~powershell
gpyj
~~~

单轮自然语言调用：

~~~powershell
gpyj run -p "帮我选股票"
gpyj run -p "从半导体行业里选股票"
gpyj run -p "从 AI 算力板块里选"
~~~

在连续对话中可以继续输入：

~~~text
继续预测首选
预测刚才的第二只备选
~~~

旧的直接分析、直接预测业务子命令已经移除，避免命令参数与自然语言形成两套用户流程。设置、登录和退出命令只负责运行环境，不是第三项研究功能。

## 6. 内部实现职责

| 文件 | 职责 |
|---|---|
| `agent/src/ashare/peizhi.py` | 量化配置的唯一加载与语义校验入口 |
| `agent/src/ashare/wangluo_kehu.py` | Windows 直连/系统代理、重试、错误分类和来源尝试 |
| `agent/src/ashare/dongcai_api.py`、`tengxun_api.py` | 东方财富、腾讯公开数据适配及响应事实核验 |
| `agent/src/ashare/fanwei_faxian.py` | 实时目录发现、候选匹配与范围详情核验 |
| `agent/src/ashare/shichang_shuju.py` | 交易日历、市场时钟、横截面、实时快照、成分与批量行情 |
| `agent/src/ashare/macd_jiegou.py` | 零轴、交叉、柱体动能、顶底背离和失效状态的纯计算 |
| `agent/src/ashare/fenxi_yinzi.py` | 八组分析因子、同行样本和确定性证据 |
| `agent/src/ashare/fenxi_xingtai.py` | 涨停回马枪有限状态机 |
| `agent/src/ashare/fenxi_weipan.py` | 尾盘阶段、动态均线和分钟证据 |
| `agent/src/ashare/xuangu_fanwei.py` | 全市场与已核验命名范围的候选池策略 |
| `agent/src/ashare/xuangu_fenxi.py` | 过滤、深度复核、风险与统一排序编排 |
| `agent/src/ashare/dangu_fenxi.py` | 单股身份、完整日线、统一量化与买入结论编排 |
| `agent/src/ashare/dangu_lianghua.py` | 建立有界比较池并复用统一选股量化流水线 |
| `agent/src/ashare/dangu_juece.py` | 不改分数地将统一推荐资格适配为单股买入结论 |
| `agent/src/ashare/macd_huifang.py` | 结构事件历史回放、交易约束、无结构基线与稳定性统计 |
| `agent/src/ashare/moxing_gongju.py` | 可复用的模型纯函数与验证辅助能力 |
| `agent/src/ashare/dangu_yuce.py` | 既有 T+1/T+2/T+3 训练、验证与预测 |
| `agent/src/tools/gupiao_analysis_state.py` | 进程内会话交接仓储；只保存候选身份和分析证据，不保存市场时间序列 |
| `agent/src/tools/` | 智能体内部的分析、预测两项业务契约 |
| `agent/src/agent/context.py` | 智能体决策边界、结果解释、异常展示和会话衔接 |

内部工具会使用短期进程内会话信息连接分析与预测，但该信息不是市场数据缓存，不含行情时间序列，进程重启即失效，也不应出现在自然语言回答中。

## 7. 已知边界

- 公开行情、财务和板块数据可能延迟、限频或缺字段；
- 日 K 与 5 分钟数据不能反映盘口排队、公告冲击和实际成交；
- 当前可取得的行业或板块成员不一定能完整还原历史成分变化；
- 盘中确认始终是暂定状态，最终以完整收盘数据为准；
- 指数平滑异同移动平均线结构是滞后指标证据，拐点确认会天然延迟；虽已具备无未来数据的回放器，但尚未取得满足生产基线和时点行业覆盖门槛的真实历史增益结论；
- 排名、预测、参考价格和区间都不能保证收益；
- 程序不替用户决定交易价格、仓位或是否交易。
