# 程序介绍：个人 A 股日 K 分析与三交易日预测

## 1. 程序定位

本程序是个人使用的中国大陆 A 股日 K 研究工具，不是通用量化平台。它只做两个动作：

1. **方向分析**：分析一只股票未来涨跌的可能性，给出证据和风险提示。
2. **三日预测**：预测同一只股票从最近完整收盘日开始的 T+1、T+2、T+3 三个实际交易日。

两个动作组成唯一闭环：

```text
gupiao_fenxi（事实、证据、方向可能性、analysis_id）
             |
             v
gupiao_yuce（同一 analysis_id，一次返回 T+1/T+2/T+3）
```

这里的“可能性”在第一阶段表现为当前因子证据的方向性倾向；第二阶段的概率和预测才使用模型或历史样本统计估计。两者都不构成买卖建议、收益保证或目标价。

程序运行时需要访问外部数据源。除这两个研究功能外，程序没有其它业务，也不执行自动交易、券商接入、分钟级预测或超过 T+3 的预测；缓存只是内部取数加速。

## 2. 第一项功能：方向分析

入口是 `gupiao_fenxi`，CLI 对应 `gpyj gupiao <代码或名称>`。

程序先锁定最近一个已确认的完整收盘日 T，再读取和整理：

- 日 K 价量、收益、均线、动量、RSI、MACD、ATR、波动和量能；
- 可取得的股票资料、财务指标和估值；
- 停牌、ST、涨跌幅限制、流动性和数据新鲜度；
- 同行历史面板、市场状态和横截面相对强弱；
- 数据源、复权方式、警告、缺失字段和风险。

### 2.1 量化因子分组

分析阶段会计算因子工程注册表中的 8 个经济组（当前注册表共 89 个候选因子），并在公开结果的 `factor_analysis.groups` 中逐组返回可用数量、当前因子值、同行同日分位数和解释。分组含义如下：

| 因子组 | 主要观察内容 |
|---|---|
| 趋势结构 | 均线结构、趋势斜率、趋势拟合质量、金叉速度 |
| 动量与反转 | 1/3/5/10/20 日收益、RSI、MACD DIF 和柱体 |
| K 线压力 | 跳空、日内收益、收盘位置、实体、上下影线和收盘压力 |
| 价量确认 | 量比、成交额异常、成交额冲击、价量相关、量价残差 |
| 突破与回撤质量 | 突破距离、放量确认、回撤质量、停滞压力、低量长下影 |
| 相对强弱 | 相对同行、行业、全市场和沪深 300 的超额收益、收益排名 |
| 风险与流动性 | ATR、波动率、振幅、回撤、成交额、换手率和流通市值 |
| 市场背景 | 市场/行业平均收益、上涨广度、收益离散度和市场状态 |

`factor_evidence` 是面向解释的逐组文字证据；`score_0_100` 和 `same_date_peer_percentiles` 只表示当前证据强弱或相对位置，不是上涨概率。同行样本不足时，分位数会为空，但该组仍会报告已取得的原始因子数量和“信息有限”。

输出中的 `direction_analysis` 是面向用户的紧凑结论：

| 字段 | 说明 |
|---|---|
| `horizon` | 当前因子状态；完整三日数值由第二项功能返回 |
| `direction` | `偏上涨`、`偏下跌` 或 `中性/不确定` |
| `positive_probability` | 固定为 `null`；第一阶段不训练概率模型 |
| `negative_probability` | 固定为 `null`；完整概率只在第二阶段返回 |
| `probability_method` | 固定为 `null`；因子证据不换算成概率 |
| `evidence_label` | 证据偏正面、偏负面、中性或不足 |
| `confidence` | 当前证据完整度的解读标签；预测模型质量只在第二阶段返回 |
| `reasons` | 形成结论的证据文字 |

第一阶段只整理当前因子、技术面、基本面和风险证据，不训练收益或方向概率模型。技术分、基本面分和其它启发式分数只负责解释证据，不能直接换算成概率；校准概率和历史样本比例只在第二阶段三日预测中使用。普通终端会在原始量化依据之外追加一轮 LLM 通俗总结，但总结必须覆盖上述 8 个因子组，不能只复述均线、RSI 或 MACD。

第一阶段还返回 `analysis_id`。它是同一进程内的短期内存编号，用来把事实分析和第二阶段预测绑定起来；进程重启或分析时点变化后必须重新分析。

## 3. 第二项功能：未来三个交易日预测

入口是 `gupiao_yuce`，CLI 对应 `gpyj yuce <代码或名称>`。它只接受：

```json
{"analysis_id": "fx_..."}
```

一次调用固定返回三个期限，每项包括：

| 字段 | 说明 |
|---|---|
| `target_trade_date` | 对应的实际交易日 |
| `direction` | `up`、`down` 或 `flat_or_unavailable` |
| `positive_probability` / `negative_probability` | 上涨/下跌可能性 |
| `predicted_return` / `predicted_return_pct` | 相对 T 收盘的累计收益估计 |
| `predicted_close` | 从 T 收盘推导的参考收盘价 |
| `interval_80` | 参考价格区间（若模型可得） |
| `validation_passed` | 该周期是否通过样本外门槛 |
| `confidence` | 该周期的模型质量标签 |

预测以 T 收盘为基准，使用真实交易日历，不把自然日当交易日，不生成 T+4 或更远期限。价格参考值会遵守可取得的 A 股涨跌幅边界，但不模拟盘口、排队或实际成交。

如果目标交易日、数据新鲜度、同行样本或模型训练不满足条件，工具返回 `status=unavailable` 及原因；不会用自然日推算，也不会补写数字。单个周期未通过验证时，仍可发布模型估计，但 `validation_passed=false` 且可信度降低。

## 4. 模型和因子实现（已完成）

单股模型不对目标股票自身历史做小样本自拟合，而是构造目标股票与同行的日 K 面板，在时间顺序上滚动训练和验证。

当前实现包含：

- 原始日 K 价量、动量、均线、波动、成交量和 ATR 因子；
- 市场、同行和横截面相对收益、宽度、离散度、排名与规模因子；
- `agent/src/ashare/yinzi_gongcheng.py` 中的因子清洗、缺失处理、去极值、稳定性筛选、同组去重和训练窗内特征工程；
- HistGradientBoostingRegressor 与稳健缩放 Ridge 的小型集成；
- 第二阶段使用独立方向模型、历史样本外正收益比例和概率校准；
- 经验、分位数和共形区间（按可用性选择参考区间）；
- 扩展窗口、标签跨界清除和最终保留窗口的滚动样本外验证；
- 以 `confidence`、`validation_passed`、`data_health` 和 `risks` 公开质量与限制。

程序内部只读取根目录 `lianghua_peizhi.json` 中的数据和模型参数；它不是产品入口，也不提供额外业务开关。预测期限由程序固定为 `[1, 2, 3]`，频率固定为日线。

## 5. 数据与网络行为

### 必须联网

Tushare 和 AKShare 提供行情、交易日历、基本面和必要的横截面数据。`source=auto` 通常优先 Tushare，在个股日线复权不可用时按工具策略降级 AKShare；结果会保留实际来源、复权方式、分析日期、新鲜度、警告和错误。

数据源限频、权限不足、空字段或行情过旧时，程序会降低结果状态或停止输出，不把旧数据说成当前数据。

### 内部缓存

日线 SQLite 仓库和短期文件缓存只用于减少重复网络请求、批量读取历史日 K 和支持增量续取。它们没有业务命令入口，也不产生额外业务结果或交易动作；程序始终按联网来源链取数。

## 6. 入口与调用契约

### CLI

```powershell
gpyj gupiao 600519.SH
gpyj yuce 600519.SH --json
gpyj run -p "分析 600519.SH 的涨跌可能性"
gpyj
```

`gpyj settings`、`gpyj openai-login` 和 `gpyj openai-logout` 是运行配置命令，不是业务功能。CLI 不提供额外研究业务子命令。

连续对话中输入 `/exit` 保存当前会话并退出；`Ctrl+C` 可中断当前输入。退出清理只关闭终端状态刷新，不会触发行情读取或模型训练。

### MCP

`agent/mcp_server.py` 只注册：

- `gupiao_fenxi(gupiao, source?, history_calendar_days?)`；
- `gupiao_yuce(analysis_id)`。

MCP 可以使用 stdio 或本机 HTTP；它只服务个人研究，不连接券商、不保存账户凭据、不提交订单。

### 智能体路由

`agent/src/agent/context.py` 规定：新股票先调用 `gupiao_fenxi`；用户需要具体未来数值时，再用同一 `analysis_id` 调用一次 `gupiao_yuce`。LLM 只能解释工具结果，不能自行补写概率、价格、区间或方向。

## 7. 主要代码职责

| 文件 | 已完成职责 |
|---|---|
| `agent/src/tools/gupiao_fenxi_tool.py` | 隐藏内部量化底稿，发布方向分析和 `analysis_id` |
| `agent/src/tools/gupiao_yuce_tool.py` | 一次性整理 T+1/T+2/T+3 公共字段 |
| `agent/src/tools/gupiao_analysis_cache.py` | 进程内短期保存两阶段关联结果 |
| `agent/src/ashare/gupiao_yanjiu.py` | 数据时点、行情/基本面整理、技术证据和总装配 |
| `agent/src/ashare/dangu_yuce.py` | 同行面板、因子、训练、验证和未来三日底稿 |
| `agent/src/ashare/yinzi_gongcheng.py` | 因子工程与稳定性控制 |
| `agent/src/ashare/moxing_pinggu.py` | 基准比较与证据质量辅助函数 |
| `agent/cli.py` | Windows/PowerShell 入口 |
| `agent/mcp_server.py` | MCP 两工具入口 |

## 8. 结果边界与已知限制

- 公开数据可能延迟、限频或缺字段；
- 日 K 不能反映盘口深度、盘中成交、公告冲击和突发事件；
- 同行池使用当前可取得的成员资料，历史成员变化不一定能完整回溯；
- 三日预测是统计估计，不能保证收益，也不是目标价；
- 程序不替用户决定是否交易、用什么价格交易或采用何种仓位。

## 9. 维护原则

后续只接受围绕这两个功能的修正：数据源兼容、数据时点、日 K 因子、模型质量、结果展示和错误处理。不得借维护之名增加第三项业务或平台化入口。
