# 项目设计与实现：维护约束

> 本文只记录维护边界、改造触发条件和文档同步规则。已经完成的功能、调用契约、输出字段、模型行为和使用方式统一放在 [程序介绍.md](./程序介绍.md)；安装与快速使用见 [README.md](./README.md)。

## 1. 项目定位

这是一个个人使用的中国大陆 A 股日 K 研究程序，业务上只有两件事：

1. 分析一只股票涨跌的可能性并说明证据；
2. 预测同一只股票未来三个实际交易日。

程序运行时需要联网。除上述两项外，不增加其它业务，不把研究结果扩展为交易执行或平台服务。

## 2. 改造触发条件

只有以下情况需要继续改造：

- Tushare、AKShare 或交易日历接口变化；
- 复权、完整收盘时点、预测目标日期等口径错误；
- 日 K 因子、训练稳定性、样本外验证或概率校准的明确缺陷；
- 两阶段结果展示、错误信息和数据质量披露问题；
- 用户实际操作后发现的可复现 bug。

每一项改动都必须继续满足：

- 业务仍只有方向分析和三日预测；
- 预测集合仍严格为 T+1/T+2/T+3；
- 不引入第三个工具、额外 CLI/MCP 业务入口或新的用户参数；
- 不让模型或 LLM 自行补造价格、概率、区间和证据；
- 不连接证券账户、不提交委托、不自动交易；
- 不把内部缓存变成独立业务结果。

没有上述明确触发条件时，不继续扩展功能。

## 3. 文档同步规则

修改代码后按以下顺序同步：

1. 先更新 [程序介绍.md](./程序介绍.md) 中与已实现行为对应的说明；
2. 再更新 [README.md](./README.md) 中的使用方式、入口或定位；
3. 只有存在新的未完成维护事项时，才更新本文；
4. 删除过时的字段、命令、文件名和示例，不用“历史上支持”继续留在用户文档里；
5. 检查工具注册表、MCP、CLI、提示词和三份文档的工具名与参数是否一致。
