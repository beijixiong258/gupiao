# 项目设计与实现

本文记录统一选股分析改造后的维护边界、依赖方向和扩展规则。用户行为与结果口径见 [程序介绍.md](./程序介绍.md)，安装和快速使用见 [README.md](./README.md)。

## 1. 设计目标

项目面向个人 A 股研究，不建设通用量化平台。业务顶层固定为：

1. 从全市场或用户用日常语言描述的实时数据源范围中分析并筛选合格股票；
2. 展示量化分析后主动询问；仅在用户明确确认时，对合格候选预测 T+1、T+2、T+3。

实现必须同时满足：

- 自然语言是唯一用户入口；
- 数据时点、规则状态和失败原因可追溯；
- 分析排序与预测概率严格分离；
- 本地不持久化市场数据，预测确认后现取远端数据并训练；
- 数据获取、规则计算、业务编排、模型训练和展示各自单一职责；
- 公共能力只保留一份实现，业务模块之间不导入对方的私有函数；
- 抽象只服务当前重复、耦合或错误边界，不预建平台能力。

最上层设计原则是“智能体负责理解、审查与决策，量化程序负责可复现计算”。行业、板块、概念和同义词不能成为静态业务枚举，也不能靠不断增长的条件分支模拟自然语言理解。智能体可以结合上下文判断真实候选、来源质量和歧义，但不能改写因子数值、排名、概率或风险门槛。该原则同时记录在根目录 `AGENTS.md`，后续会话和其他智能体开发时必须优先遵守。

## 2. 依赖方向

依赖只能从业务编排指向稳定的底层能力：

~~~text
自然语言智能体 / MCP
        |
        v
分析工具 ------------------ 预测工具
        |                        |
        v                        v
统一选股编排门面           三交易日预测编排门面
        |                        |
        v                        v
已核验范围策略 / 无状态规则 同行样本 / 预测训练服务
        |                        |
        +-----------+------------+
                    v
       因子 / 形态 / 尾盘 / 模型算法门面
                    |
                    v
             配置 / 远端市场数据
~~~

约束如下：

- 预测模块不得反向依赖统一选股编排；
- 分析模块不得通过预测业务文件复用行情、同行池或模型私有函数；
- 网络连接、重试与来源尝试只能由公开数据 HTTP 边界提供；
- 市场时钟、实时快照、横截面和交易日历只能由市场数据模块提供；
- 自然语言命名范围必须先经过实时目录发现和详情核验，候选池不得自行猜测行业或概念；
- 配置文件只能经统一配置模块读取和校验；
- 展示层不得重新计算或修改业务分数、概率、价格和状态。

## 3. 采用的设计方式

### 3.1 门面模式：统一选股编排

`XuanguFenxiFuWu` 是分析业务门面。它负责串联范围解析、硬过滤、批量因子、形态、尾盘、深度复核和排序，但把具体数据读取与规则计算委托给独立模块。调用方不需要知道流水线细节。

门面只负责编排，不保存跨请求全局状态；请求内状态由数据上下文持有。

### 3.2 策略模式：候选池范围

`HouXuanChiCelue` 定义候选池契约，当前只有全市场和已核验命名范围两种策略。后者接收范围发现服务产生的领域值对象，不接收大模型猜出的 `industry`、`board` 等分类字符串。增加范围规则时应扩展独立策略，而不是在主流程中继续堆叠大型条件分支。

当前业务不建立动态插件系统，也不为数据源的分类体系创建面向用户的固定枚举。

### 3.3 发现与核验：智能体决策前的事实边界

`fanwei_faxian.py` 每次按需读取东方财富当前行业与概念目录，先生成真实候选，再统一去除用户口语后缀并计算匹配。唯一精确候选仍必须通过官方详情页核对代码、名称和类别；多个真实候选交给智能体结合上下文判断，无法可靠判断时才产生一次结构化澄清。目录不可用、核验冲突和来源降级均是结构化状态，不能被转换成“用户输入错误”或“该范围不存在”。

这个边界不是固定工作流替代智能体：程序只提供实时事实、确定性匹配证据和来源质量，最终的语义判断、是否需要澄清以及如何向普通用户解释仍由智能体负责。

### 3.4 请求上下文：数据复用和时点一致性

`FenxiShujuShangxiawen` 在单次请求内复用交易日历、市场阶段、实时快照、横截面、板块成分、批量日线和分钟线。它解决两个实际问题：

- 同一批数据被多个规则重复拉取；
- 同一分析中的数据来自互相冲突的时点。

上下文不是通用依赖注入容器，也不跨请求持久化业务结果。

### 3.5 有限状态机：形态生命周期

涨停回马枪采用枚举与结果数据类表达“调整、缩量、等待突破、盘中确认、收盘确认、失效、过期”。每次转换都由交易日位置和明确条件决定，避免使用多个布尔值拼接出矛盾状态。

尾盘阶段同样使用枚举区分初筛、盘中暂定、等待收盘和收盘复核，盘中证据不能冒充完整日线结论。

### 3.6 函数式核心：因子和模型公共能力

因子定向、分数组装、模型特征构造、滚动验证、校准和区间估计尽量保持为输入明确、无网络副作用的普通函数。远端网络访问集中在数据边界，业务层不读写本地市场数据，便于理解数据流，也避免再次形成超大业务文件。

不为简单字典建立无收益的类层级；只在状态、范围和请求上下文等能减少错误的边界使用 `Enum`、`dataclass` 或协议。

### 3.7 适配门面：隔离模型内部实现

`moxing_xunlian_api.py` 是训练服务与底层模型算法之间的窄门面。训练服务只导入公开算法名称，底层历史私有函数只在该门面中适配；因此模型辅助模块可以继续整理实现，而不会把大量内部名称泄漏到预测业务编排。

该门面只解决当前已存在的跨模块私有依赖，不引入运行时容器、动态注册或额外状态。

### 3.8 会话仓储：分析与预测的最小交接

`AnalysisSessionStore` 只保存最近分析的公开结果、合格候选身份和预测所需的确定性分析证据，用于理解用户后续的“继续预测首选”。它是有界、线程安全、随进程结束而失效的会话仓储，不保存 DataFrame、行情时间序列、估值历史或模型样本。

预测确认后仍必须重新访问远端数据源。会话仓储不能演变为市场缓存，也不能用于跨进程恢复陈旧预测上下文。

### 3.9 结构化澄清：契约与展示适配器

`ClarificationRequest`、`ClarificationChoice` 和 `ClarificationAnswer` 只描述一个问题、最多四个预设选项和用户回答，不依赖 Rich、CLI 或具体模型。`RichChengqingTishi` 负责 Windows 终端中的编号窗口、自由输入和进度显示暂停；模型侧的 `clarify` 工具只负责提交结构化请求，不直接读取终端。

预测确认由分析结果确定性构造，并在完整分析答案显示后交给终端展示。用户选择再作为新的用户回合送回智能体，因此同轮预测硬拦截无需放宽。范围歧义可以由模型调用同一澄清适配器；单轮命令和 MCP 没有交互回调时不注册该工具，继续使用文字询问。这一交互工具不属于第三项研究业务。

## 4. 模块职责

| 模块 | 单一职责 |
|---|---|
| `peizhi.py` | 加载根目录量化配置，并校验权重、阈值、时间与数量关系 |
| `wangluo_kehu.py` | 统一国内公开数据的直连/系统代理、超时、重试、错误分类与安全来源记录 |
| `dongcai_api.py` | 适配东方财富目录、成分与历史接口，处理分页和响应结构 |
| `tengxun_api.py` | 适配腾讯当前前复权日线，并提取同响应证券快照供事实核验 |
| `fanwei_faxian.py` | 动态发现行业/概念目录、匹配候选并使用详情页二次核验 |
| `shichang_shuju.py` | 提供规范化代码、交易日历、市场阶段、实时/日线横截面、成分和批量历史 |
| `gupiao_yanjiu.py` | 提供历史行情、技术指标和基本面等底层研究能力 |
| `fenxi_yinzi.py` | 组装八组分析因子、横截面证据和基本面分 |
| `tonghang_yangben.py` | 准备预测所需同行候选、相对快照和历史样本 |
| `fenxi_xingtai.py` | 计算涨停回马枪状态、得分、原始数值和失败原因 |
| `fenxi_weipan.py` | 计算尾盘阶段、动态均线、分钟量价、VWAP 和证据可信度 |
| `xuangu_fanwei.py` | 定义范围值对象以及全市场、已核验命名范围候选池策略 |
| `xuangu_guize.py` | 提供无状态硬过滤、风险计分、综合评分和结果投影规则 |
| `xuangu_fenxi.py` | 编排候选池、批量因子、增强证据、深度复核和最终结果 |
| `moxing_gongju.py` | 提供分析与预测可共用的纯模型辅助函数 |
| `moxing_xunlian_api.py` | 隔离训练服务与模型辅助模块的内部实现名称 |
| `yuce_xunlian.py` | 实现 T+1/T+2/T+3 特征筛选、滚动验证和模型训练 |
| `dangu_yuce.py` | 编排预测数据、交易日历、训练调用、风险解释和结果发布 |
| `tools/gupiao_analysis_state.py` | 保存有界的进程内分析会话交接信息，并拒绝 DataFrame/Series 进入状态 |
| `tools/` | 定义智能体内部的两阶段业务契约、短期会话关联和可选澄清工具 |
| `agent/clarification.py` | 定义平台无关的澄清契约，并从分析结果构造预测选择请求 |
| `duihua/chengqing_ui.py` | 将澄清请求适配为 Rich 编号窗口并规范化用户输入 |
| `agent/context.py` | 约束自然语言路由、历史结果兼容与用户可见表达 |

原 `bankuai_yuce.py` 中可复用的模型能力已迁移到公共模块，旧板块预测业务文件已经删除。同行样本、模型训练和预测发布现已分层；统一选股也已拆分为范围策略、无状态规则和业务编排，业务门面之间不再共享私有实现。

## 5. 核心业务不变量

### 5.1 时点

- 交易日身份只由交易日历确定，不能退化为星期判断；
- 正式日线因子只能使用已完成交易日；
- 14:30 后实时证据单独标记，不覆盖正式日线字段；
- 15:00–15:05 不发布伪完整收盘结论；
- 所有候选在同一轮分析中使用一致的基准日期和横截面。

### 5.2 过滤与排名

- 硬风险过滤在评分前执行，不能靠低分替代；
- 八组日 K 因子是主体，形态和尾盘是附加证据；
- 缺失证据从有效权重中移除并降低可信度；
- 风险分独立扣除，不能被正向证据稀释；
- 综合分只用于排序，不得命名或解释为上涨概率；
- 未同时达到分数与可信度门槛时不得强制推荐。

### 5.3 预测

- 只能预测当前分析中的合格候选；
- 分析和预测必须分成两个用户回合，禁止分析成功后自动训练；
- 初始分析请求中的预先授权不算确认，必须先展示量化结果并单独询问一次；
- 交互终端在完整结果显示后提供最多四个编号预设项，选择作为新的用户回合；用户也可直接输入合格备选名称或代码；
- 非交互客户端中，用户在文字询问后回复“是/行/继续/预测吧”即完成确认并默认指向首选，不得再做第三次确认；
- 确认后重新下载目标、同行、估值和宽基数据，不读取或写入本地市场数据缓存；
- 期限固定为 T+1、T+2、T+3；
- 分析排名不得改变训练标签、概率或预测值；
- 模型无法满足数据和验证条件时必须透明返回不可用或低可信度。

## 6. 配置规则

可调阈值集中在根目录 `lianghua_peizhi.json`：

- `fenxi`：候选数量、历史门槛、流动性、组件权重、因子组权重和推荐门槛；
- `xingtai`：涨停价容差、缩量窗口、突破期限、量能阈值和各状态得分；
- `weipan`：阶段时间、量价目标区间、市值范围、回撤上限和分钟候选数量；
- 既有数据、因子和预测模型配置保持原职责。

`peizhi.py` 是唯一读取入口。新增阈值时必须同时增加语义校验，避免在业务代码中散落魔法数字。仅属于不可配置业务定义的常量可以保留在对应规则模块中，并使用有含义的名称。

## 7. 数据访问和性能边界

- 市场数据只来自当前远端请求；禁止读写本地行情文件、因子缓存或 SQLite 行情仓库；
- 命名范围必须动态读取当前行业与概念目录，并对会改变候选池的关键事实执行详情页或第二可靠来源核验；
- 腾讯前复权日线是当前分析主源，证券身份和最新价、量、精确成交额由同响应快照核对；东方财富与 Tushare 是受控备用源；
- 交易日历优先 Tushare，限频时使用新浪交易所日历；权威来源全部失败时不得以工作日近似；
- Windows 国内数据连接模式只能是明确直连或系统代理；直连使用不继承环境代理的客户端，AKShare 兼容调用同时屏蔽 Windows 系统代理；
- 全市场实时快照在一次请求中最多读取一次；
- 历史日线按交易日从远端批量获取并在请求内复用；
- 禁止在全市场候选循环中逐只请求远端日线；
- 分钟线只对配置限定的靠前候选读取；
- 深度基本面和技术复核只对最终少量候选执行；
- 分析结果展示并获得用户明确确认后，才重新下载目标与同行的长期模型窗口；不能直接把选股短窗口当作预测训练窗口；
- 数据源降级、字段缺失和时点不一致必须进入结果。

业务结果固定区分 `recommendation`、`no_recommendation`、`clarification_required`、`data_unavailable` 和程序错误。外层运行状态必须继承该结论，不能因为模型生成了一段说明文字就把 `unavailable` 标记为成功。模型回答若混入不符合当前中文交互预期的脚本，会由展示边界拦截并生成安全的中文降级说明。

运行记录不属于市场缓存，但仍按隐私类别治理：默认 `metadata_only`，不保存提示词、模型正文或完整工具载荷，并受 14 天、100 次、100 MB 三重保留上限约束。对话持久化移除量化工具大结果，只保留文字与跨进程后必须重新分析的标记；分析与预测的完整交接仅存在当前进程的有界内存仓储中。

如果需要新增证据，优先复用请求上下文已有字段；只有现有数据不能表达该证据时才扩展数据模块。

## 8. 维护与扩展步骤

修改分析规则时：

1. 先确认规则属于硬过滤、八组因子、形态、尾盘、基本面还是风险；
2. 在对应单一职责模块中实现纯计算，返回原始数值、命中条件和失败原因；
3. 需要阈值时先扩展集中配置及校验；
4. 由统一编排接入，不在工具、CLI 或提示词中复制业务计算；
5. 同步更新 [程序介绍.md](./程序介绍.md) 与 [README.md](./README.md)；
6. 删除被替代的旧分支，避免新旧逻辑长期并存。

修改预测时必须单独确认是否会改变既有模型、标签、期限或输出口径；分析侧需求不能顺带改变预测行为。

## 9. 明确不引入

项目当前不引入：

- 微服务、消息队列、分布式任务或服务发现；
- 通用策略 DSL、动态插件平台或复杂依赖注入容器；
- 多租户、权限、审计、管理后台或券商交易接口；
- 没有实际复用对象的仓储层、接口层或抽象工厂；
- 为设计模式数量而增加的类和包装层；
- 与分析和三交易日预测无关的业务入口。

保持普通函数、小型数据类和明确模块即可；不为市场数据引入 SQLite、文件缓存或通用仓储框架。每个新抽象都必须能指出它消除的当前重复、耦合或状态错误。
