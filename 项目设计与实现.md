# 项目设计与实现

本文记录单股完整证据分析与范围规则选股的维护边界。安装见 [README](./README.md)，用户行为见 [程序介绍](./程序介绍.md)，公式与样本限制见 [量化指标说明](./量化指标说明.md)。

## 1. 设计目标

项目面向个人 A 股研究，保留两条业务路径：点名单股时提取本次能分析的完整证据；要求选股时，在核验后的范围内依据公开条件筛选研究候选。两条路径共享指标和数据能力，职责与失败语义分别明确。

不再存在人为综合评分、组件与因子组权重、风险扣分、置信度分或推荐分数门槛；单股没有强制买卖结论。三交易日预测、模型训练、预测资格交接和确认流程已经移除。

智能体负责理解意图、检查范围歧义与来源质量、解释证据冲突；量化程序负责可复现的行情处理、指标、形态、可交易性检查、显式条件和非支配分层。智能体不能改写数值、编造概率或绕过筛选条件。用户不需要理解内部工具、数据源类别和参数。

## 2. 依赖方向

```text
自然语言智能体 / MCP
          |
          v
   股票分析诊断工具
          |
          v
单股证据编排 / 范围选股编排
          |
          v
范围发现与核验 / 有界比较池 / 无状态规则
          |
          v
日线指标 / MACD结构 / 基本面 / 形态 / 尾盘 / 补充诊断
          |
          v
   配置 / 远端市场数据
```

网络连接、重试和来源尝试集中在公开数据边界；交易日历、快照、横截面和请求内复用集中在市场数据上下文；配置只有一个加载入口。展示层消费结构化证据与统一指标定义，不重算业务指标、条件或层次。

优先使用普通函数、小型数据类和组合。抽象必须消除当前已有的重复、耦合或状态错误，不预建插件平台、通用仓储或复杂依赖注入容器。

## 3. 两条业务路径

### 3.1 单股证据编排

`dangu_fenxi.py` 负责身份、市场时点、目标日线、技术分析和最终报告。`dangu_lianghua.py` 复用因子面板、基本面、形态、尾盘和补充诊断，并构造有界比较池。同行与市场参照是可选证据，不是启动本股分析的门槛。

目标股票不经过范围选股的候选过滤器。ST、退市风险、上市时长、流动性或当前成交限制应成为单股的风险与可交易性证据，不得让目标股票在报告前消失。仅有短历史时，按每项指标自己的窗口计算；无法计算的字段保留明确缺失原因。配置不设置额外的单股最低历史行数或最低同行数。

目标身份、必要交易日期或目标日线无法取得时，返回 `unavailable` 或需要澄清的真实原因。可选来源失败、部分指标窗口不足和实际日线落后于请求日期时，保留其他已取得证据并返回 `partial / information_partial`。技术程序错误为 `error / program_error`，不能与正常证据缺口混淆。

核心结果包括：

| 字段 | 含义 |
|---|---|
| `diagnosis_summary` | 已算事实的中文摘要、支持证据、反证、复评条件 |
| `technical_summary` | 完整技术结果，不压缩成技术分 |
| `daily_factor_analysis` | 八组原始指标、中文定义、可用数与缺失字段 |
| `fundamental_analysis` | 全部可用基本面、估值和比较样本证据 |
| `limit_up_pullback_pattern` | 形态状态、原始值、适用性与失败原因 |
| `late_session_analysis` | 尾盘阶段、条件和分钟证据 |
| `supplemental_diagnostics` | 七个补充块的摘要、原始指标和缺失原因 |
| `tradability`、`realtime_snapshot` | 当前成交限制、核验状态与快照 |
| `data_analysis` | 最新完整日线、历史范围、解析资料和比较资料等未在其他组件重复的原始信息 |
| `evidence_gaps`、`risks`、`warnings` | 缺失、限制及其解释边界 |
| `diagnosis_validity`、`data_provenance` | 日线截止日、生成时间、盘中状态及来源 |

兼容字段 `recommendation_available=False` 只表示单股功能不发布选股候选，不得翻译为“暂不建议买入”。`primary` 与 `alternatives` 在单股结果中为空。已删除买入适配模块，不能通过提示词或展示逻辑重新建立旧结论。

### 3.2 范围选股编排

`XuanguFenxiFuWu` 串联范围核验、基础过滤、流动性抽样、日线获取、原始因子、显式条件、非支配分层及受限深度复核。

基础过滤检查证券规则、风险警示、上市时长、停牌、有效价量、一字涨停、最低真实成交额和历史质量。主流程先在受限样本上计算条件并排序，再对限定数量的股票补充分钟、深度财务与技术证据；复核后重新核验条件并分层，不能保留过时的通过状态。

显式条件由 `xuangu_guize.pinggu_shangzhang_tiaojian` 统一计算：

| 条件 | 必需事实 |
|---|---|
| `trend_alignment` | 收盘价高于 MA20，且 MA5 高于 MA20 |
| `positive_momentum` | 5 日收益、20 日收益和 MACD 柱均大于零 |
| `outperform_csi300` | 20 日相对沪深 300 超额收益大于零 |
| `volume_confirmation` | 5 日均量 / 20 日均量不低于 1 |
| `bounded_volatility` | 20 日年化波动不超过配置上限 |
| `technical_review` | 深度技术复核返回有效必需指标，没有程序错误 |
| `tradability` | 当前适用的成交检查和必要实时核验通过 |

每条记录 `key`、`label`、`status`、`values`、`reason`。状态分别为 `met`、`unmet`、`unavailable`；缺失不是通过，也不是数值零。`eligible`、`missing_conditions` 和 `unmet_conditions` 汇总这些事实，不计算命中率分数。

`paixu_shangzhang_houxuan` 使用五维原始值分层：20 日相对沪深 300 超额、5 日收益、20 日收益、20 日波动和完整日线真实成交额。波动较低为该维更优，其他四维较高为该维更优；全部不劣且至少一维更优才构成支配。同层按成交额降序、代码稳定排列，不声称具有更高上涨概率。缺少比较字段者公开缺口，不能以零补齐后参与比较。

候选投影保留 `selection_analysis`、`meets_selection_conditions` 和全部诊断组件。`selection_analysis` 含实际条件、比较值、金额口径、非支配层和 `ranking_basis`。`rank` 仅是展示序号。

范围结果继续使用 `primary`、`alternatives` 兼容字段，用户界面将其解释为研究候选。无合格候选时，完成深度复核且没有技术程序错误的结果最多保留一个 `diagnostic_candidates` 观察对象，必须强调没有通过条件；观察对象不能被叫作首选或备选。范围技术程序错误时整体报告错误，已复核候选及原因保留在结构化结果中。

### 3.3 抽样和比较范围

全市场与已核验命名范围是候选池策略；单股是独立业务路径。范围采用行业与市场流动性分散抽样，受 `prefilter_limit`、`factor_candidate_limit`、`deep_analysis_limit` 等数量限制。单股比较池优先真实同行，再按成交额补充有界市场参照。

`candidate_counts`、`filter_summary`、`selection_methodology` 和来源信息说明输入、过滤、抽样、日线可得、深度复核及合格数量。结论只对本次实际样本成立，不把全市场入口描述成逐股遍历，也不宣称筛出了全市场最可能上涨的股票。

## 4. 数据与领域边界

### 4.1 动态范围发现

`fanwei_faxian.py` 按需读取当前行业与概念目录，再对真实候选做匹配和详情核验。命名范围策略接收核验结果，不接收大模型猜测的数据源分类。

唯一精确候选必须核对名称、代码和类别；多个真实候选由智能体结合上下文判断，仍含糊时才澄清一次。成功、冲突、失败和因请求限额尚未核验的候选都保留。核验失败不能作为排除另一个范围的事实，也不能把其余成功候选自动当作唯一答案。

### 4.2 请求内数据上下文

`FenxiShujuShangxiawen` 只在一次调用内复用资料、日历、市场阶段、横截面、快照、基本面、成分、批量日线和分钟线。已失败请求也可在本次上下文记忆，避免重复失败。上下文不持久化市场数据，不跨请求保存业务事实。

交易日历覆盖请求历史区间。显式日历不足时返回覆盖不足，不先截短行情再声称完整。Tushare 客户端初始化与接口调用均属于可记录的来源失败，失败后仍尝试既有新浪备用源；所有权威日历失败时，不退化为星期判断。

日线按同一请求质量要求逐源验收，检查交易日期、有效行、覆盖和最新完整交易日。非空但不合格的数据也会在既有限额内进入备用链；采用单一来源的完整复权序列，不拼接不同来源价格。范围的历史质量门槛用于候选筛选；单股较短的可用历史用于逐项提取证据，未覆盖部分照实披露。

### 4.3 原始指标和中文定义

`yinzi_gongcheng.py` 的统一注册表与 `factor_definition` 提供 `label`、`unit`、`meaning`、`display_scale`。`fenxi_yinzi.py` 逐组透传 `values`、`metric_definitions`、`missing_fields`、`available_factor_count` 与 `factor_count`。这些数量描述字段可得性，不是置信度分。

指标不再先定向、排名再加权形成组分；原始值和本身有意义的横截面分位均可保留。中文定义只维护一份，展示层不另建整套指标字典。新增指标须有可复现定义、单位与缺失规则，不能只加入名称而没有可解释内容。

真实历史换手缺失时，不用成交额相关性代替。行业证据按交易日和真实标签分组，至少五个有效观测才发布对应统计。比较池广度仅代表本次样本；当前标签回看历史不能冒充历史成员重建。

### 4.4 形态和尾盘状态

涨停回马枪用明确状态表达调整、缩量、等待突破、盘中确认、收盘确认、失效和过期，返回原始值及失败原因，不返回状态分。

尾盘区分初筛、盘中暂定、等待收盘、收盘复核。正式因子只使用已完成日线；动态均线和盘中金额单独标记。14:30 前不适用尾盘，14:45 后按配置取得少量分钟数据，15:00–15:05 不发布伪完整收盘结果。

### 4.5 MACD 结构和历史研究

`macd_jiegou.py` 消费统一入口生成的特征，组合零轴、相邻交易日交叉、柱体动能、价格拐点、快线/柱体背离和失效检测。价格与指标变化按相对尺度处理。默认左右各二日确认的背离以真实右侧确认日为信号日期，不能回写到拐点。

结构结果提供支持证据、反证、风险和失效原因，不产生独立评分，也不能单独决定候选资格。MACD 柱的正负用于显式动量条件，与额外的结构解释分别保留。

`macd_huifang.py` 是独立研究能力，只接收内存历史面板，不自行下载和保存市场数据。回放按实际确认日、次一市场交易日入场，保留停牌、封板、整手、真实成交额参与率、滑点和成本的影响。不使用未来失效状态改写信号日证据。

基线 `baseline_selected` 必须来自未使用待评估结构字段的历史时点流程。报告保留大盘、行业、相对强弱、价量、范围、时间与参数稳定性及覆盖缺口。缺少经时点核验的行业成员、真实金额或足够跨度时不能宣布验证完成；研究报告不自动修改生产筛选规则，不训练或发布行情预测。

### 4.6 七类补充诊断

`buchong_zhenduan.py` 与 `caiwu_zhenduan.py` 复用本次历史和财务，计算下行风险、ATR 偏离、CMF、流动性稳定性、增长、现金流符号与报告时点。输出 `supplemental_diagnostics` 的状态、时点和 `blocks`；块含 `key`、`label`、`status`、`summary`、`metrics`、`missing_reason`。

补充块直接返回诊断事实，不保留评分用途字段。缺失进入 `evidence_gaps` 和复评条件，不增加隐含筛选条件。CMF 不是账户资金净流入，未核验单位的现金流字段只解释符号。EMA、ATR、CMF 的公式权重仍是正常数学定义，不属于人为综合评分。

## 5. 工具、对话和展示

MCP 是同一分析工具的薄包装，股票名称/代码、范围与数量直接交给业务入口。`ClarificationRequest` 等小型值对象负责一个问题、选项与回答，Windows Rich 适配器负责界面。没有交互回调的单轮命令和 MCP 使用普通文字澄清。

对话系统提示区分单股解释与范围筛选。保留有效当前结果用于本轮解释，已持久化会话只保留文字和需要重新分析标记；旧预测阶段、旧评分结果和不可恢复分析标记不能当成当前证据。

`fenxi_zhanshi.py` 构造共享确定性证据报告。单股、返回候选和观察对象共用组件展示，八组每项数值、单位、中文含义与缺失均可见；技术、财务、形态、尾盘、补充指标和来源不按固定条数截断。未知字段保持可见原名，不编造定义。模型省略某块时补齐规范内容，避免追加重复报告；不靠强制买卖标签、分数或全篇数字关键词检查来决定成功。

`dangu_zhaiyao.py` 同时供单股与范围候选使用，消费既有技术、因子、形态、尾盘和补充诊断，整理证据背景与分歧，不重算指标或改变候选次序。`evidence_context` 和 `evidence_conflicts` 是公开观测关系，不是内部思维链。当前有效信号的失效条件进入复评条件；不适用形态和历史过期状态保留查阅，不混入当前支持或风险。

`shuju_yuan.py` 的行情时点核验统一约束可交易性和尾盘，编排层也据此决定是否允许快照参与盘中形态。原始快照保留用于说明来源；未核验价格不代替完整日线诊断参考价。行情适配读取逐股来源时间，元数据中的空日期不再覆盖行级有效日期。详见《量化指标说明》的行情与估值口径。

状态语义如下：

| `status / outcome` | 含义 |
|---|---|
| `ok / analysis_success` | 单股完成当前证据分析 |
| `partial / information_partial` | 单股保留可分析证据，部分来源或指标缺失 |
| `ok / recommendation` | 范围取得通过条件的研究候选，内部名称不代表买入指令 |
| `ok / no_recommendation` | 范围没有股票通过本次条件 |
| `clarification_required` | 身份或范围需要澄清 |
| `unavailable / data_unavailable` | 必要数据不能支持当前业务 |
| `error / program_error` 或其他错误结果 | 计算或执行失败，保留原因 |

外层运行状态必须继承业务结果。`partial` 不能改称完整成功，`unavailable` 和 `error` 也不能因为模型产生一段文字就显示为成功。

## 6. 配置与模块职责

阈值集中在根目录 `lianghua_peizhi.json`，由 `peizhi.py` 唯一加载并作语义校验：

- `fenxi`：范围抽样、历史质量、上市时长、流动性、波动上限、返回数量与 MACD 结构/研究参数；
- `dangu`：请求历史窗口、含目标在内的比较池上限与同行优先数量；
- `xingtai`：涨停容差、缩量窗口、突破期限与量能条件；
- `weipan`：阶段时间、分钟候选数量、量比与回撤条件；
- `shuju`、`wangluo`、`run_logs`：频率、请求节奏、网络边界和记录保留上限。

不再保留评分权重、状态分或推荐分数门槛配置。新增可调阈值须同步校验；固定业务定义使用有含义的常量，不能在工具、提示词和展示层各写一份。不得自行修改版本号。

| 模块 | 职责 |
|---|---|
| `wangluo_kehu.py` | 直连/系统代理、超时、重试、错误分类与来源记录 |
| `dongcai_api.py`、`tengxun_api.py` | 公开接口适配与响应核验 |
| `fanwei_faxian.py`、`xuangu_fanwei.py` | 实时范围发现、核验和候选池策略 |
| `shichang_shuju.py` | 市场时点和请求内数据上下文 |
| `gupiao_yanjiu.py` | 历史行情、技术与基本面底层能力 |
| `yinzi_gongcheng.py`、`fenxi_yinzi.py` | 统一指标定义、原始八组与横截面证据 |
| `macd_jiegou.py`、`macd_huifang.py` | 结构检测与独立历史研究 |
| `fenxi_xingtai.py`、`fenxi_weipan.py` | 形态和尾盘条件 |
| `buchong_zhenduan.py`、`caiwu_zhenduan.py` | 七类补充证据 |
| `dangu_fenxi.py`、`dangu_lianghua.py` | 单股完整证据与可选比较证据编排 |
| `xuangu_guize.py`、`xuangu_fenxi.py` | 硬过滤、显式条件、非支配层及范围编排 |
| `tools/`、`agent/context.py`、`agent/loop.py` | 分析契约、智能体边界、业务状态与会话 |
| `agent/fenxi_zhanshi.py` | 共享完整报告与漏项补齐 |

## 7. 数据访问和性能边界

市场数据只来自本次远端请求，禁止行情文件、因子缓存或 SQLite 市场仓库。腾讯前复权日线为当前分析主源，身份和最新价量额由同响应快照核对；东方财富和 Tushare 是受控备用。交易日历优先 Tushare，再尝试新浪；每个来源的失败和降级都进入结果。

Windows 国内数据默认直连，可明确配置系统代理；直连客户端不继承环境或系统代理。一次请求最多读取一次全市场实时快照。同行和范围日线使用批量接口及请求内复用，不在全市场循环里逐股无限请求；分钟、深度财务和技术复核均有数量上限。

运行记录不是市场缓存，但按类别治理：默认 `metadata_only`，不保存提示词、模型正文或完整工具载荷，受 14 天、100 次、100 MB 上限约束。配置、凭据、对话文字与重新分析标记可以按现有规则持久化；不能从旧对话恢复旧行情并称为当前事实。

## 8. 维护与扩展

新增证据时，先确认已有输入是否足够，在所属计算模块提供原始值、单位、含义、窗口与缺失原因；复用统一注册表和报告入口，再考虑确有必要的新数据访问。新增范围条件须明确状态与真实阈值，不通过“多命中一些条件”暗中重新引入评分。

修改应同步长期文档，验证只覆盖直接影响范围，删除被替代的旧分支。临时脚本、日志和中间产物按用户规定移入垃圾桶，不永久删除、不保留无要求的备份。不引入与当前股票研究无关的业务、券商交易接口、微服务、消息队列或没有实际复用对象的抽象层。
